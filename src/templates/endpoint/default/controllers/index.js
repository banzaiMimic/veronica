const ihnUtils = require('@informaticahn/utils')
const log = ihnUtils.logger.log

const [ACTION][ENTITY] = require('../useCases').[ACTION][ENTITY]

const [METHOD][ACTION][ENTITY] = require('./[METHOD][ACTION][ENTITY]')({ [ACTION][ENTITY], log }).[METHOD][ACTION][ENTITY]

module.exports = {
  [METHOD][ACTION][ENTITY]
}
