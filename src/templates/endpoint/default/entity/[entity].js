const buildMake[ENTITY] = ({}) => {
  const make[ENTITY] = params => {
    const new[ENTITY] = {
      ...params
    }
    return new[ENTITY]
  }

  return Object.freeze({
    make[ENTITY]
  })
}

module.exports = buildMake[ENTITY]