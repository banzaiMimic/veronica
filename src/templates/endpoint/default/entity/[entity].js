const buildMake[ENTITY] = ({}) => {
  const make[ENTITY] = ({
    param
  }) => {
    const new[ENTITY] = {
      param
    }
    return new[ENTITY]
  }

  return Object.freeze({
    make[ENTITY]
  })
}

module.exports = buildMake[ENTITY]
