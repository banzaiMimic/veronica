const make[ENTITY] = require('../entity')

const make[ACTIONUPPER][ENTITY] = () => {

  const [ACTION][ENTITY] = async () => {
    try {
      // const someEntity = make[ENTITY]
    } catch (e) {
      /*
        log.error({
          event: 'make[ACTIONUPPER][ENTITY] error',
          message: e.message
        })
      */
    }
  }

  return Object.freeze({
    [ACTION][ENTITY]
  })

}

module.exports = make[ACTIONUPPER][ENTITY]
